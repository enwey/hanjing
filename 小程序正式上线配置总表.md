# 小程序正式上线配置总表

本文档按“文件路径 / 参数名 / 当前状态 / 上线目标值 / 是否必填 / 风险说明”整理，用于正式上线前逐项核对。

## 1. 小程序前端配置

### 1.1 接口环境

文件路径：`/Users/apple/Desktop/WorkSpace/hanjing/mp-weixin/api/request.js`

| 参数名 | 当前状态 | 上线目标值 | 是否必填 | 风险说明 |
| --- | --- | --- | --- | --- |
| `apiUrls.develop` | `http://127.0.0.1:5005/api/v1` | 保持开发本地地址 | 否 | 仅开发环境使用，不能用于正式版 |
| `apiUrls.trial` | `https://test-api.hanjing.com/v1` | 确认为体验版可用地址 | 否 | 体验版不可误指向生产或失效域名 |
| `apiUrls.release` | `https://api.hanjing.com/v1` | 确认为正式生产接口地址 | 是 | 正式版发布后所有请求会走这里 |
| `ENABLE_REAL_API` | `true` | 保持 `true` | 是 | 如果关闭，真实接口不可用 |

### 1.2 小程序项目配置

文件路径：`/Users/apple/Desktop/WorkSpace/hanjing/mp-weixin/project.config.json`

| 参数名 | 当前状态 | 上线目标值 | 是否必填 | 风险说明 |
| --- | --- | --- | --- | --- |
| `appid` | `wxc03d5e23b11114bc` | 确认是正式小程序 AppID | 是 | 如果不是正式 AppID，会发错小程序 |
| `setting.urlCheck` | `false` | 建议改为 `true` | 强烈建议 | 关闭后上传时不会严格校验域名配置 |
| `setting.uploadWithSourceMap` | `true` | 建议改为 `false` | 强烈建议 | 会把源码映射一起上传，不适合正式版 |
| `setting.minified` | `true` | 保持 `true` | 是 | 正式包建议压缩 |
| `setting.minifyWXSS` | `true` | 保持 `true` | 是 | 正式包建议压缩 |
| `setting.minifyWXML` | `true` | 保持 `true` | 是 | 正式包建议压缩 |
| `libVersion` | `3.15.2` | 确认与提审/正式环境兼容 | 是 | 基础库不兼容会导致页面异常 |

### 1.3 小程序全局声明

文件路径：`/Users/apple/Desktop/WorkSpace/hanjing/mp-weixin/app.json`

| 参数名 | 当前状态 | 上线目标值 | 是否必填 | 风险说明 |
| --- | --- | --- | --- | --- |
| `permission.scope.userLocation.desc` | 已配置“用于查找离您最近的门店” | 与实际用途保持一致 | 是 | 位置权限用途必须与隐私指引一致 |
| `requiredPrivateInfos` | `["getLocation"]` | 与微信后台隐私声明保持一致 | 是 | 平台声明不一致可能影响审核 |
| `navigateToMiniProgramAppIdList` | 已配置 2 个 AppID | 确认都是正式允许跳转的小程序 | 否 | 错配会导致跳转失败或审核问题 |

### 1.4 微信公众平台后台配置

这部分不在仓库文件中，但正式上线必须配置。

| 配置项 | 当前状态 | 上线目标值 | 是否必填 | 风险说明 |
| --- | --- | --- | --- | --- |
| `request` 合法域名 | 需人工确认 | 至少包含 `https://api.hanjing.com` | 是 | 不配置会导致网络请求失败 |
| `uploadFile` 合法域名 | 需人工确认 | 至少包含上传接口域名 | 是 | 病历附件、文件上传会失败 |
| `downloadFile` 合法域名 | 需人工确认 | 如有下载能力需配置 | 建议 | 下载报告或附件会失败 |
| `webSocket` 合法域名 | 需人工确认 | 至少包含 `wss://api.hanjing.com` 对应域名 | 是 | 在线客服 WebSocket 无法连接 |
| 隐私保护指引 | 需人工确认 | 与位置、录音、手机号、病历上传等实际能力一致 | 是 | 审核和合规高风险项 |
| 订阅消息模板 | 需人工确认 | 预约成功、提醒等模板可用 | 建议 | 订阅提醒功能无法正常触发 |

## 2. 后端环境变量配置

建议正式环境单独维护生产 `.env`，不要直接复用本地开发配置。

参考模板文件：`/Users/apple/Desktop/WorkSpace/hanjing/hanjing-clinic-backend/.env.example`

### 2.1 数据库与服务基础配置

主要读取文件：
- `/Users/apple/Desktop/WorkSpace/hanjing/hanjing-clinic-backend/db.js`
- `/Users/apple/Desktop/WorkSpace/hanjing/hanjing-clinic-backend/server.js`

| 参数名 | 当前状态 | 上线目标值 | 是否必填 | 风险说明 |
| --- | --- | --- | --- | --- |
| `DB_HOST` | `.env.example` 为 `127.0.0.1` | 改为正式数据库地址 | 是 | 指向本地会导致线上后端无法连接数据库 |
| `DB_PORT` | `.env.example` 为 `3306` | 改为正式数据库端口 | 是 | 端口错误将无法启动 |
| `DB_USER` | `.env.example` 为 `root` | 改为正式数据库账号 | 是 | 不建议生产使用 root |
| `DB_PASSWORD` | `.env.example` 为 `root` | 改为正式数据库密码 | 是 | 生产默认密码风险极高 |
| `DB_NAME` | `.env.example` 为 `hanjing_clinic` | 确认为正式数据库名 | 是 | 连错库会污染或读取错误数据 |
| `PORT` | 默认 `5005` | 与 nginx / 网关转发保持一致 | 是 | 端口不一致会导致外网不可访问 |
| `NODE_ENV` | 本地未固定 | 必须为 `production` | 是 | 影响支付 mock、运行模式、安全逻辑 |

### 2.2 安全密钥配置

主要读取文件：
- `/Users/apple/Desktop/WorkSpace/hanjing/hanjing-clinic-backend/helpers.js`

| 参数名 | 当前状态 | 上线目标值 | 是否必填 | 风险说明 |
| --- | --- | --- | --- | --- |
| `JWT_SECRET` | 代码里有默认值 | 显式配置强随机密钥 | 是 | 不配置会使用默认值，登录令牌安全性不足 |
| `PII_ENCRYPTION_KEY` | 代码里有默认值 | 显式配置 32 位强密钥 | 是 | 不配置会使用默认值，手机号/身份证加密不安全 |

### 2.3 微信小程序开放能力配置

主要读取文件：
- `/Users/apple/Desktop/WorkSpace/hanjing/hanjing-clinic-backend/routes/admin.js`

| 参数名 | 当前状态 | 上线目标值 | 是否必填 | 风险说明 |
| --- | --- | --- | --- | --- |
| `WX_MINI_APP_ID` | `.env.example` 留空 | 填正式小程序 AppID | 是 | 获取微信 access_token 失败 |
| `WX_MINI_APP_SECRET` | `.env.example` 留空 | 填正式小程序 AppSecret | 是 | 微信开放接口无法调用 |

### 2.4 微信支付配置

主要读取文件：
- `/Users/apple/Desktop/WorkSpace/hanjing/hanjing-clinic-backend/routes/client.js`

| 参数名 | 当前状态 | 上线目标值 | 是否必填 | 风险说明 |
| --- | --- | --- | --- | --- |
| `WECHAT_APP_ID` 或 `WX_APPID` | 未见生产值 | 填正式支付对应小程序 AppID | 是 | 微信下单失败 |
| `WECHAT_PAY_MCH_ID` | 未见生产值 | 填正式商户号 | 是 | 微信支付不可用 |
| `WECHAT_PAY_SERIAL_NO` | 未见生产值 | 填商户证书序列号 | 是 | 无法签名下单 |
| `WECHAT_PAY_PRIVATE_KEY` | 未见生产值 | 填商户私钥内容 | 是 | 无法签名下单 |
| `WECHAT_PAY_API_V3_KEY` | 未见生产值 | 填 APIv3 密钥 | 是 | 支付回调无法解密 |
| `WECHAT_PAY_NOTIFY_URL` | 未见生产值 | 填正式 HTTPS 回调地址 | 是 | 通用支付回调失败 |
| `WECHAT_APPOINTMENT_PAY_NOTIFY_URL` | 未见生产值 | 填预约支付正式 HTTPS 回调地址 | 是 | 预约支付状态无法回写 |

### 2.5 支付安全开关

主要读取文件：
- `/Users/apple/Desktop/WorkSpace/hanjing/hanjing-clinic-backend/routes/client.js`
- `/Users/apple/Desktop/WorkSpace/hanjing/hanjing-clinic-backend/ecosystem.config.cjs`

| 参数名 | 当前状态 | 上线目标值 | 是否必填 | 风险说明 |
| --- | --- | --- | --- | --- |
| `ALLOW_CLIENT_APPOINTMENT_PAY_CONFIRM` | PM2 生产配置为 `false` | 保持 `false` | 是 | 若为 `true`，前端可能绕过回调直接确认支付 |
| `ENABLE_MOCK_WECHAT_PAY` | 未固定 | 不配置或明确关闭 | 是 | 开启后生产环境可能误走 mock 支付 |

### 2.6 外网地址与告警配置

主要读取文件：
- `/Users/apple/Desktop/WorkSpace/hanjing/hanjing-clinic-backend/routes/admin.js`
- `/Users/apple/Desktop/WorkSpace/hanjing/hanjing-clinic-backend/alert-notifier.js`

| 参数名 | 当前状态 | 上线目标值 | 是否必填 | 风险说明 |
| --- | --- | --- | --- | --- |
| `PUBLIC_BASE_URL` | 未见生产值 | 填正式公网基础地址 | 建议 | 后台对外链接、资源地址可能异常 |
| `API_PUBLIC_BASE_URL` | 未见生产值 | 填正式 API 公网地址 | 建议 | 某些后台功能拼接地址可能异常 |
| `ALERT_WEBHOOK_URL` | PM2 模板留空 | 接飞书/钉钉/企业微信告警地址 | 强烈建议 | 线上报错只能靠手工看日志 |

## 3. 后端进程与部署配置

文件路径：`/Users/apple/Desktop/WorkSpace/hanjing/hanjing-clinic-backend/ecosystem.config.cjs`

| 参数名 | 当前状态 | 上线目标值 | 是否必填 | 风险说明 |
| --- | --- | --- | --- | --- |
| `name` | `hanjing-clinic-backend` | 可保持不变 | 是 | PM2 进程识别名 |
| `script` | `server.js` | 保持不变 | 是 | 启动入口 |
| `instances` | `max` | 结合服务器 CPU 评估 | 建议 | 机器小而开太多实例会加大资源压力 |
| `exec_mode` | `cluster` | 保持或按部署策略调整 | 建议 | 集群模式要确认日志与状态管理策略 |
| `watch` | `false` | 保持 `false` | 是 | 生产不建议 watch |
| `max_memory_restart` | `1G` | 按服务器内存评估 | 建议 | 过低会频繁重启，过高不利于兜底 |
| `env_production.NODE_ENV` | `production` | 保持 `production` | 是 | 正式环境必需 |
| `env_production.PORT` | `5005` | 与网关保持一致 | 是 | 外网转发依赖 |
| `env_production.ALLOW_CLIENT_APPOINTMENT_PAY_CONFIRM` | `false` | 保持 `false` | 是 | 支付安全关键项 |
| `env_production.ALERT_WEBHOOK_URL` | 空 | 建议配置 | 强烈建议 | 无法自动接收线上告警 |
| `error_file` | `./logs/pm2_error.log` | 确认目录存在可写 | 是 | 否则日志落盘失败 |
| `out_file` | `./logs/pm2_access.log` | 确认目录存在可写 | 是 | 否则日志落盘失败 |

## 4. 日志与运行文件

主要文件：
- `/Users/apple/Desktop/WorkSpace/hanjing/hanjing-clinic-backend/server.js`
- `/Users/apple/Desktop/WorkSpace/hanjing/hanjing-clinic-backend/alert-notifier.js`

| 项目 | 当前状态 | 上线目标值 | 是否必填 | 风险说明 |
| --- | --- | --- | --- | --- |
| `requests.log` | 服务运行时持续写入 | 配置日志轮转与磁盘监控 | 建议 | 长期运行会持续增长 |
| `alerts.log` | 告警写本地文件 | 配合 webhook 一起使用 | 建议 | 只写本地不利于及时发现问题 |
| `logs/pm2_error.log` | PM2 模板使用 | 确认目录和权限 | 是 | PM2 报错日志丢失 |
| `logs/pm2_access.log` | PM2 模板使用 | 确认目录和权限 | 是 | PM2 访问日志丢失 |

## 5. 上线前建议手动切换或复核的开发项

| 文件路径 | 参数名 / 项目 | 当前状态 | 上线目标值 | 是否必填 | 风险说明 |
| --- | --- | --- | --- | --- | --- |
| `/Users/apple/Desktop/WorkSpace/hanjing/mp-weixin/project.config.json` | `urlCheck` | `false` | 建议 `true` | 强烈建议 | 正式域名校验被绕过 |
| `/Users/apple/Desktop/WorkSpace/hanjing/mp-weixin/project.config.json` | `uploadWithSourceMap` | `true` | 建议 `false` | 强烈建议 | 源码映射随包上传 |
| `/Users/apple/Desktop/WorkSpace/hanjing/hanjing-clinic-backend/helpers.js` | `JWT_SECRET` 默认值 | 存在默认值 | 必须显式配置 | 是 | 生产安全风险 |
| `/Users/apple/Desktop/WorkSpace/hanjing/hanjing-clinic-backend/helpers.js` | `PII_ENCRYPTION_KEY` 默认值 | 存在默认值 | 必须显式配置 | 是 | 敏感数据保护不足 |

## 6. 正式上线最终核对清单

- [ ] 正式小程序 AppID 与 `project.config.json` 一致
- [ ] 微信公众平台已配置 `request` 合法域名
- [ ] 微信公众平台已配置 `uploadFile` 合法域名
- [ ] 微信公众平台已配置 `webSocket` 合法域名
- [ ] 生产 API 域名 `https://api.hanjing.com/v1` 可正常访问
- [ ] 生产数据库已准备好，且不是测试数据
- [ ] `JWT_SECRET` 已替换为正式强密钥
- [ ] `PII_ENCRYPTION_KEY` 已替换为正式强密钥
- [ ] `WX_MINI_APP_ID` / `WX_MINI_APP_SECRET` 已配置
- [ ] 微信支付全部参数已配置完成
- [ ] `WECHAT_PAY_API_V3_KEY` 已配置
- [ ] 支付回调地址为正式 HTTPS 地址
- [ ] `NODE_ENV=production`
- [ ] `ALLOW_CLIENT_APPOINTMENT_PAY_CONFIRM=false`
- [ ] `ENABLE_MOCK_WECHAT_PAY` 未开启
- [ ] `logs` 目录存在且可写
- [ ] 告警 webhook 已配置
- [ ] 微信隐私指引与实际功能保持一致
- [ ] 订阅消息模板已配置并验证
